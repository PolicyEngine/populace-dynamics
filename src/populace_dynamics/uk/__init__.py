"""UK dynamics layer (ADR 0002).

Ports the panel machinery from archived policyengine-uk-data#346 onto
a light three-table dataset contract, decoupled from policyengine-uk,
with the blockers from that PR's review fixed (orphaned-entity pruning
after any person removal; weight-proportional immigration donors).
"""
