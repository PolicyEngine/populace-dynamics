# M6 candidate-2 decision-9 re-adjudication proposal

Status: **PROSPECTIVE PROGRAM-AMENDMENT PROPOSAL — NO RE-ADJUDICATION,
REGISTRATION, OR BYTE CHANGE YET**.

This proposal asks the fable referee to reopen only
`docs/design/m6_candidate2_program.md` §10 decision 9 after the completed
remarriage learning ladder.  It presents original disposition 2's
registration with a candid modal-failure forecast and one precise
further-evidence alternative.  It does not choose between them.

## 1. Operative state and amendment boundary

The ratified round-6 finding reproduced and ratified R0, recorded
`NO_OP_DESIGNED_PAUSE`, and ended the reduction question with a
byte-reproduced no-op.  Decision 9 was previously adjudicated to the
train-only disposition; under its frozen no-op fallback, that completed result
resolves to the designed pause.  Section 9.3 box 4 therefore continues to
forbid registration 8 unless and until decision 9 is prospectively
re-adjudicated
(`docs/design/m6_candidate2_program.md:971-976,1038-1046`).

The program is silent on reopening an adjudicated §10 decision: it contains
no reopen or re-adjudication provision, and §9.2's ceremony matrix names no
route for revising an adjudicated §10 outcome
(`docs/design/m6_candidate2_program.md:944-955`).  "Re-adjudication" here is
therefore not a program-supplied retry.  It is shorthand for a later
prospective amendment to program law under the standing prospective-ceremony
discipline.  That construction follows the W1 standing rule that an amendment
binds only runs registered after ratification
(`docs/amendments/gate_w1_amendment_1_family_b_di_bands.md:331-335`) and the
later prospective ceremonies in PRs #242, #244, and #246, which amended the
ratified round-2 plan's reducer contract without editing its existing bytes.

This draft is the prospective process for changing that program disposition.
Opening the draft changes nothing.  Only an adversarial referee decision,
fixes, verification, and Max's standing ratification ceremony can replace the
current decision-9 pause.

The sibling surface question in draft PR #249 proceeds regardless.  It is the
fixed rung required by
`docs/design/m6_remarriage_learning_plan_round2.md:788-799`, which states:

> Any surface change needs its own prospective §2.8 and floor ceremony and
> cannot retroactively change either learning result.
>
> [...]
>
> The pause continues throughout.

Those sentences bind the surface ladder under current program law.  This
proposal asks whether the registration disposition should change
prospectively now that the learning mechanism has run.  It neither cancels nor
delays the surface question.  Neither choice in this proposal, nor a later
surface result, can rewrite either train-only result.

Even under choice R below, R0 and `NO_OP_DESIGNED_PAUSE` remain the
law-learning disposition: no nonzero law is forced, and the frozen §7
surface/third-family ladder continues.  Choice R would change only the
separate registration consequence of decision 9.

## 2. The original decision was reasonable when made

The candidate-2 program presented three honest choices
(`docs/design/m6_candidate2_program.md:856-872`):

1. a train-only remarriage delta through a follow-up proposal;
2. registration with a candid modal-failure forecast; or
3. a designed pause.

It recommended the first.  The reason was explicit: a forecast-only run would
"knowingly accept nearly zero remarriage margin," while "A pause alone has no
learning mechanism."  Option 2 would knowingly spend the candidate-2 one-shot
on a cell whose published evidence reached `105.8%` of tolerance and supplied
no mechanism to improve it.

That adjudication was not mistaken.  At the time, option 1 offered new
train-only information that options 2 and 3 did not.  Re-adjudication is
appropriate because that factual premise has now changed, not because the
original referee should have predicted the later result.

## 3. What changed: the learning mechanism ran to exhaustion

The authorized ladder is completed and ratified:

- round 1 tested its pre-registered train-only family and selected no change;
- the round-2→6 investigation replaced the foreclosed global family with a
  redesigned per-origin family and ultimately ratified no change;
- the round-2 exposure guard proved load-bearing and produced the designed
  rule-5 no-op rather than being relaxed after outcome contact;
- after the raw round-3 selector, the per-origin finding traversed six reducer
  amendment/finding PRs—#242, #243, #244, #245, #246, and #247—before
  ending in independent byte reproduction of R0 and
  `NO_OP_DESIGNED_PAUSE`; and
- the ratified reduction records `no_eligible_nonzero_law`, not an undefined
  selection or an unreviewed incident.

Across the two rounds, every named candidate law in the completed ladder was
published, counted, and subjected to its frozen selector.  No nonzero law
cleared the frozen eligibility and selection contract.  The result is a
ratified no-eligible-improvement certificate for the investigated families.
That phrase means exactly `no_eligible_nonzero_law` under the frozen
conjunctive selector.  Individual diagnostics may improve without making a law
eligible.  The certificate is not a proof that no imaginable remarriage law
could improve, and it does not authorize a forced additional nonzero law.

For this governance question, the cell is now a fully characterized residual,
not an uncharacterized fragile unknown.  The published post-repair record
names `remarriage.18-64`, a score band of
`0.263–0.427` against the unchanged `0.403` tolerance, approximately
`65.3–105.8%` of the bar, and same-direction overshoot across repaired seeds
(`docs/design/m6_candidate2_program.md:842-872,1038-1046`).  Full
characterization does not make the margin safe.  It makes the risk candidly
forecastable.

The new governance fact is therefore:

> the learning mechanism that justified preferring option 1 has run to its
> ratified no-op endpoint, while the published band and candid modal-FAIL
> posture remain.

## 4. The re-adjudication question

To ratify this proposal, the referee must choose exactly one of these two
prospective dispositions.  The referee may instead reject the proposal and
identify a precise correction or evidence condition.  This proposal makes no
recommendation between R and E.

| Choice | Honest case | Honest cost | Registration effect if ratified |
|---|---|---|---|
| **R — re-adjudicate to original disposition 2** | The prescribed train-only mechanism is exhausted, the investigated families have a ratified `no_eligible_nonzero_law` certificate, and registration would produce candidate-2 outcome evidence while the independent surface inquiry continues. | It knowingly spends the one-shot with a named cell whose upper measured band exceeds tolerance, potentially before the surface referee answers. Modal FAIL remains the candid forecast; a later surface change cannot regrade the run but may limit the continuing certification value of evidence produced under the pooled contract. | Decision 9 no longer bars registration 8 once every other §9.3 box is satisfied. The registration must contain §5's exact modal-failure language. |
| **E — require one further surface finding first** | The referee may reasonably require candidate-blind evidence that the pooled cell itself is an adequate certification surface before accepting its forecasted failure risk. | Candidate-2 evidence is delayed; the surface inquiry may instead require a prospective gate ceremony or return to model-law learning. It does not itself improve the model. | Registration 8 remains forbidden under decision 9 until the exact evidence in §6 is ratified and decision 9 is re-adjudicated again. |

The original bare-pause cost remains relevant, but the current pause is not
evidence-free: the plan's fixed surface question is active and proceeds under
either choice.  Choice E names this proposal's concrete further-evidence
condition.  If the referee accepts neither R nor E, the review should name any
different condition prospectively; until a revision is ratified, the current
designed pause remains operative.

## 5. Choice R: mandated registration forecast

If choice R is ratified, the candidate-2 registration must include the
following language at forecast prominence, not in a footnote:

> **Modal result forecast: FAIL.** The named modal-failure cell is
> `remarriage.18-64`. Its published post-repair measured forecast band is
> `0.263–0.427` against the unchanged `0.403` tolerance
> (`65.3–105.8%` of tolerance), with same-direction overshoot across repaired
> seeds. The band straddles tolerance: modal FAIL is not certain, and a
> realized PASS remains possible. Round 1 selected incumbent L0; the completed
> per-origin round selected R0 and ended in a byte-reproduced
> `no_eligible_nonzero_law` certificate for the investigated families. The
> certificate supports no forced delta and predicts neither a pass nor a
> certain fail. Candidate 2 spends its one-shot and publishes regardless.

This proposal would additionally freeze these registration disclosure fields:

```text
forecast_modal_result: FAIL
forecast_modal_failure_cell: remarriage.18-64
published_measured_score_band: [0.263, 0.427]
unchanged_gate_tolerance: 0.403
published_approx_upper_band_share_of_tolerance: 1.058
published_measured_direction: same-direction overshoot across repaired seeds
selection_disposition: NO_OP_DESIGNED_PAUSE
reduction_outcome: RATIFIED_NO_OP_DESIGNED_PAUSE
publish_regardless: true
```

The approximate share is the source program's published percentage from its
unrounded calculation, not a recomputation from the displayed rounded band
endpoints.  These are disclosure fields, not new thresholds, selectors, or
acceptance rules.  The completed candidate is still graded under the locked
gate and five-cell regression block.  A realized PASS remains possible; a
realized FAIL remains modal.  Either result is published once.

If the forecast realizes as a scored FAIL on `remarriage.18-64`, that result
adds almost nothing about that cell: its post-repair band is already measured,
candidate 2 carries no remarriage delta, and R0 remains the remarriage law
regardless.  The run's information value instead concentrates in the first
candidate-2 fresh-seed evidence for the locked joint two-delta candidate across
the other ten live gate cells.  Five of those cells are also tested by the
separate original-threshold must-not-regress block
(`docs/design/m6_candidate2_program.md:87-101,874-898,917-931`).  The
cell-by-seed scores show which other cells clear or fail under the registered
joint candidate and whether all five former passes clear their original
tolerances together on at least four seeds.  Those scores remain evidence even
if one cell defeats the gate conjunction.

The conjunction-FAIL headline alone teaches only that fewer than four seeds
cleared every live cell.  The per-cell scores can identify contributing cells,
but this one joint, non-ablated run cannot certify either model delta or a
partial surface, causally assign an outcome to either delta, identify a repair,
or establish how a repaired remarriage law would score.  The published scores
permit arithmetic with the remarriage cell omitted, but that post-hoc ten-cell
regrade is not an authorized changed-surface result.  It cannot validate a
different surface or license tuning against post-2014 outcomes.  Spending the
one-shot also forecloses rerunning these same deltas under this contract; any
further scored evidence requires a prospectively ratified successor contract.

Choice R reopens registration only after every other applicable §9.3
precondition has cleared.  It does not deem the first-marriage fit converged,
ratify the earnings amendment or floor resolution, close a remaining
conformance audit, or post registration itself.

## 6. Choice E: one precise further-evidence condition

If the referee is not prepared to ratify choice R now, the required additional
evidence is exactly:

> a ratified `POOLED_SURFACE_STANDS` disposition from the candidate-blind
> `remarriage.18-64` surface question in
> `docs/design/m6_remarriage_surface_question.md` (draft PR #249), supported by
> its cutoff-safe truth-only artifact and independent reproduction.

That finding would establish that the incumbent pooled control remains
power/non-vacuity ready on the prescribed truth instruments and that the
certification claim remains aggregate working-age flow.  It would not predict
a candidate pass, change the measured band, or alter the no-op certificate.

Under choice E:

- `POOLED_SURFACE_STANDS` returns decision 9 for this same re-adjudication,
  with choice R still requiring an explicit referee vote;
- `SURFACE_CHANGE_WARRANTED` routes through its separate prospective §2.8,
  truth-only floor, reproduction, amendment, and lock ceremony before any new
  surface can govern a registration; and
- `RETURN_TO_MODEL_LAW_LEARNING` opens only the plan's third-family proposal
  gate and supplies no automatic law or registration authority.

Only `POOLED_SURFACE_STANDS` satisfies E.  `SURFACE_CHANGE_WARRANTED`,
`RETURN_TO_MODEL_LAW_LEARNING`, `ANALYSIS_ABORT`, and an unratified surface
artifact all leave registration 8 forbidden under E, including after any
downstream surface or model-law ceremony.  A different route to registration
requires a new prospective decision-9 amendment.  Within a ratified E
disposition, the named condition may not be silently replaced after
surface-result contact; any different route requires that new amendment.

## 7. Referee and ratification ceremony

The re-adjudication belongs to the referee, under Max's standing ceremony
rules:

1. review this proposal as a prospective amendment to decision 9 only;
2. if endorsing the proposal, choose R or E and state the honest cost being
   accepted; otherwise reject it, state any exact further-evidence requirement,
   and leave the pause operative;
3. require corrections in this draft before any ratifying merge;
4. independently verify the completed ladder and quoted published band;
5. obtain Max's explicit ratification of the chosen program disposition; and
6. only then treat the amended registration rule as operative.

The proposal, review, and merge remain distinct from candidate registration
and execution.  A draft PR, favorable review, or recommendation is not itself
an adjudication.

## 8. Bytes, model deltas, and one-shot discipline remain unchanged

Neither choice changes a gate, floor, model, or run byte.  In particular, this
proposal changes no:

- cell, reducer, truth support, F6 weight, tolerance, seed, K, conjunction, or
  floor artifact;
- first-marriage or earnings implementation parameter;
- landed #226 conformance repair or remaining audit requirement;
- candidate-1 artifact, verdict, floor contract, or historical claim; or
- candidate-2 artifact path, exclusive new-file guard, environment sidecar,
  source lock, or publish-regardless rule.

The program's semantic fences also remain exact.  Neither choice may exclude
the amendment-3g class, change the first-marriage truth side, or demote either
formation cell.  Neither authorizes realized post-2014 NAWI, a change to the
pooled `CellMarginal`, its inverse, or the projected-index formula, or tuning
from the pre-repair result or the 2015–2019 residual.

Candidate 2 retains exactly the program's two model deltas:

1. the support-aware first-marriage specification and its hard pre-score fit
   abort; and
2. the prospective §2.7 stable-coordinate earnings rank-refresh mechanism.

No remarriage model delta is added.  R0 remains the current remarriage law.
The one-shot discipline remains exact: registration freezes every applicable
byte; no parameter or support rule moves afterward; a completed run writes one
fresh artifact and publishes regardless of PASS or FAIL.

## 9. The five passed-cell regression block remains unchanged

Candidate-2 acceptance still requires the live gate and the candidate-1
five-passed-cell regression block.  The block remains all five cells clearing
their original thresholds on at least four of five fresh seeds:

| Cell | Unchanged regression tolerance |
|---|---:|
| `divorce.18-44` | `0.379` |
| `incidence.20-66` | `0.404` |
| `recovery.20-66` | `0.314` |
| `earn_dlog_sd.older` | `0.269` |
| `earn_zero_rate.older` | `0.163` |

A live gate PASS with a regression-block failure remains
`GATE_PASS_REGRESSION_FAIL`, never an accepted candidate-2 PASS.  Neither R nor
E weakens that conjunction or moves a threshold.

An accepted PASS retains the program's negative certification scope: it does
not certify mortality drift, widowhood, the 2020–2022 shock window,
entrants/open-panel dynamics, lag-5 autocorrelation, stocks, report-only
AIME/PIA/claiming levels, or projection to 2100.  Every PASS claim must also
carry the program's audited-search-size limitation.

## 10. Relationship to the surface question

Draft PR #249 and this proposal answer different questions:

- the surface proposal asks what `remarriage.18-64` can honestly certify and
  follows the plan's fixed W1-style rung; and
- this proposal asks whether the completed learning ladder justifies reopening
  the program's registration disposition under the current locked surface.

The surface question proceeds regardless of R or E.  Choice R does not declare
the pooled surface sound; it accepts the current locked contract with a candid
forecast while the independent question continues.  Choice E makes one
specific surface finding a prerequisite to another decision-9 vote.

If R is ratified and candidate 2 is registered before a later surface finding,
the one-shot lock controls: no later surface amendment can change that
candidate's bytes or retroactively regrade it.  A ratified later surface change
is prospective for the next eligible registration or successor contract.

The inverse ordering is also binding.  If a surface change is ratified and
locked before candidate-2 registration, choice R's forecast under the old
pooled surface cannot authorize registration under the new contract.  The new
§2.8/floor ceremony, contract inventory, and corresponding forecast govern,
with any different decision-9 authority obtained prospectively.

## 11. Controlling law and exact amendment effect

The operative registration bar is program law.  Section 9.3 box 4 says that a
chosen pause forbids registration
(`docs/design/m6_candidate2_program.md:971-976`), keyed to §10 decision 9,
whose train-only no-op or unratifiable-law clause resolves to disposition (c),
the designed pause (`docs/design/m6_candidate2_program.md:1038-1046`).  The
ratified R0 / `NO_OP_DESIGNED_PAUSE` finding therefore engages that
program-law bar.

The round-2 plan does not create a second, freestanding registration bar.  Its
§7 sentence says that registration remains forbidden "under" exactly those
two program ranges
(`docs/design/m6_remarriage_learning_plan_round2.md:777-781`), so the sentence
is derivative and tracks a later amendment of the program disposition.  Plan
§9 says the plan does not edit candidate 2, its registry specification, or
current remarriage law, or authorize registration 8
(`docs/design/m6_remarriage_learning_plan_round2.md:833-834`), confirming that
it did not entrench an independent registration rule in either direction.

This is the same later-prospective-ceremony discipline used when ratified PRs
#242, #244, and #246 amended the round-2 plan's reducer contract without
changing the old plan bytes
(`docs/design/m6_remarriage_round4_amendment.md:48-60`;
`docs/design/m6_remarriage_round5_amendment.md:48-59`;
`docs/design/m6_remarriage_round6_amendment.md:381-399`).
The corresponding findings record each ratification
(`docs/analysis/m6_remarriage_round4_reduction.md:23-28`;
`docs/analysis/m6_remarriage_round5_reduction.md:40-47`;
`docs/analysis/m6_remarriage_round6_reduction.md:28-36`).  Git history gives
the round-2 plan the identical blob
`f99bd9898c1adcbb62b449634d9ebffadf8d3a3a` at its ratification commit
`004c57d` and the three later freezes `d63f36b`, `135b165`, and `982d2c2`.

Plan §7's freestanding commands govern only the law-learning/surface lane:
the fixed candidate-blind surface rung and its separate prospective ceremony
(`docs/design/m6_remarriage_learning_plan_round2.md:788-795`), no automatic
third family, and "The pause continues throughout"
(`docs/design/m6_remarriage_learning_plan_round2.md:797-799`).  They survive a
registration-consequence amendment untouched.  Choice R forces no nonzero
law, permits no holdout tie-break, leaves R0 and both learning results intact,
and leaves that surface ladder running; it changes only decision 9's separate
registration consequence.

If R is ratified, the operative program amendment is narrow:

- the operative outcome of §10 decision 9 changes from the ratified train-only
  no-op's designed-pause fallback to disposition 2, registration with §5's
  candid modal-failure forecast;
- only the decision-9 subcondition of §9.3 box 4 becomes satisfied; #226, the
  fixture audit, and the reconciliation requirements remain unchanged;
- the derivative plan-§7 registration sentence tracks that amended program
  disposition, so no round-2-plan §7 amendment is required, while the plan's
  law-learning pause and surface ladder remain unchanged; and
- §9.3 box 7's existing candid-forecast requirement is instantiated and locked
  with §5's exact modal-failure language and proposed disclosure fields.

Accordingly, a ratified prospective amendment of program §10 decision 9 away
from the pause removes the decision-9 bar: choice R does so directly; choice E
can do so only after a ratified `POOLED_SURFACE_STANDS` finding and a second
explicit choice-R vote.  With every other §9.3 requirement also satisfied—
boxes 2, 3, and 5; box 4's other legs; box 6; box 7, including the candid
modal-FAIL forecast; and box 8—registration 8 is unlocked.  No round-2-plan
§7 amendment is required, and no disposition in draft PR #249 unlocks
registration 8 by itself.

If E is ratified, the current decision-9 pause remains and the program records
the exact `POOLED_SURFACE_STANDS` evidence condition in §6.  Neither result is
operative in this current draft; the referee-chosen language must be carried by
the ratifying revision and merge of this proposal under this document's §7
ceremony.

This proposal does not certify a PASS, authorize registration by itself,
change candidate 2's design, reopen candidate 1, or weaken any remaining
registration precondition.  It only reopens decision 9 for an honest,
prospective referee choice in light of a completed and exhausted learning
mechanism.
